<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:system="clr-namespace:System;assembly=mscorlib">
    <system:Boolean x:Key="InvertSystemNotifyIcons">True</system:Boolean>

    <SolidColorBrush x:Key="ButtonShadow" Color="Transparent" />
    <SolidColorBrush x:Key="ButtonBorder" Color="#818181" />
    <SolidColorBrush x:Key="ButtonLight" Color="Transparent" />
    <SolidColorBrush x:Key="ButtonFace" Color="#EBEBE4" />

    <SolidColorBrush x:Key="ButtonInactiveBorder" Color="Transparent" />
    <SolidColorBrush x:Key="ButtonActiveBorder" Color="#91B1D6" />
    <SolidColorBrush x:Key="ButtonActiveBackground" Color="#A8D2FF" />
    <SolidColorBrush x:Key="ButtonHoverBorder" Color="#BBC5D9" />
    <SolidColorBrush x:Key="ButtonHoverBackground" Color="#D9E9FB" />
    <SolidColorBrush x:Key="ButtonPressedBorder" Color="#0064B3" />
    <SolidColorBrush x:Key="ButtonPressedBackground" Color="#006EC3" />
    <SolidColorBrush x:Key="ButtonFlashingBorder" Color="#FFA275" />
    <SolidColorBrush x:Key="ButtonFlashingBackground" Color="#FFB895" />

    <SolidColorBrush x:Key="TrayBorder" Color="#CDCBB9" />
    <SolidColorBrush x:Key="TrayInnerBorder" Color="#EDECE5" />
    <SolidColorBrush x:Key="TrayBackground" Color="#F3F4F0" />

    <SolidColorBrush x:Key="StartButtonBackground" Color="#E0E2DC" />
    <SolidColorBrush x:Key="StartButtonHoverBorder" Color="#6C8CB3" />
    <SolidColorBrush x:Key="StartButtonHoverBorderLight" Color="#C7DEF7" />
    <SolidColorBrush x:Key="StartButtonHoverBorderShadow" Color="Transparent" />
    <SolidColorBrush x:Key="StartButtonHoverBackground" Color="#99BFE7" />
    <SolidColorBrush x:Key="StartButtonPressedBorder" Color="#6381A5" />
    <SolidColorBrush x:Key="StartButtonPressedBorderLight" Color="#B7CDE5" />
    <SolidColorBrush x:Key="StartButtonPressedBorderShadow" Color="Transparent" />
    <SolidColorBrush x:Key="StartButtonPressedBackground" Color="#A2C1DE" />

    <SolidColorBrush x:Key="ButtonForeground" Color="#000000" />
    <SolidColorBrush x:Key="ButtonPressedForeground" Color="#ffffff" />
    <SolidColorBrush x:Key="ClockForeground" Color="#000000" />

    <SolidColorBrush x:Key="InputLanguageForeground" Color="#ffffff" />
    <SolidColorBrush x:Key="InputLanguageBackground" Color="#316ac5" />

    <SolidColorBrush x:Key="ToolTipBackground" Color="#FFFFE1" />
    <SolidColorBrush x:Key="ToolTipForeground" Color="#000000" />

    <FontFamily x:Key="GlobalFontFamily">Tahoma</FontFamily>

    <Style TargetType="ContentControl" x:Key="Taskbar" BasedOn="{StaticResource Taskbar}">
        <Setter Property="Template" Value="{DynamicResource FlatPanelTemplate}"></Setter>
    </Style>

    <Style TargetType="GroupBox" x:Key="Tray">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template" Value="{DynamicResource FlatTrayTemplate}"></Setter>
    </Style>

    <Style TargetType="Button" x:Key="TaskButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource ButtonFace}" />
        <Setter Property="Foreground" Value="{DynamicResource FlatTaskText}" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Template" Value="{DynamicResource FlatTaskTemplate}"></Setter>

    <Setter Property="Padding" Value="6,2" /><Style.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Horizontal" />
                    <Condition Binding="{Binding Path=Rows, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="1" />
                </MultiDataTrigger.Conditions>
                <MultiDataTrigger.Setters>
                    <Setter Property="Margin" Value="0,0,3,0" />
                </MultiDataTrigger.Setters>
            </MultiDataTrigger>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <DataTrigger.Setters>
                    <Setter Property="Margin" Value="0" />
                </DataTrigger.Setters>
            </DataTrigger>
        </Style.Triggers></Style>

    <Style TargetType="Button" x:Key="TaskButtonActive" BasedOn="{StaticResource TaskButton}">
        <Setter Property="Background" Value="{DynamicResource ButtonActiveBackground}" />
        <Setter Property="Template" Value="{DynamicResource FlatTaskTemplate}"></Setter>
    <Setter Property="Margin" Value="0" /><Setter Property="Padding" Value="6,2" /><Setter Property="Foreground" Value="{DynamicResource FlatTaskText}" /></Style>

    <Style TargetType="Button" x:Key="TaskButtonFlashing" BasedOn="{StaticResource TaskButton}">
        <Setter Property="Background" Value="{DynamicResource ButtonFlashingBackground}" />
        <Setter Property="Template" Value="{DynamicResource FlatTaskTemplate}"></Setter>
    <Setter Property="Margin" Value="0" /><Setter Property="Padding" Value="6,2" /><Setter Property="Foreground" Value="{DynamicResource ButtonFlashingForeground}" /></Style>

    <Style TargetType="ContentControl" x:Key="TaskButtonThumbnail">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <Border BorderThickness="1" BorderBrush="{DynamicResource ToolTipForeground}" Background="{DynamicResource ToolTipBackground}" Padding="4" Margin="4">
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ToolTip x:Key="StartButtonTip" Content="{DynamicResource start_button_tip}" />

    <Style TargetType="ToggleButton" x:Key="StartButton" BasedOn="{StaticResource StartButton}">
        <Setter Property="Template" Value="{DynamicResource FlatToggleTemplate}"></Setter>

    <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <DataTrigger.Setters>
                    <Setter Property="Margin" Value="0" />
                </DataTrigger.Setters>
            </DataTrigger>
        </Style.Triggers></Style>

    <Style TargetType="Button" x:Key="ToolbarButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource ButtonFace}" />
        <Setter Property="Margin" Value="0,0,0,-1" />
        <Setter Property="Template" Value="{DynamicResource FlatButtonTemplate}"></Setter>

    <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <Setter Property="Margin" Value="0" />
            </DataTrigger>
        </Style.Triggers></Style>

    <Style TargetType="Thumb" x:Key="ToolbarThumb" BasedOn="{StaticResource ToolbarThumb}">
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Margin" Value="4,4,0,3" />
        <Setter Property="Width" Value="2" />
        <Setter Property="Template" Value="{DynamicResource FlatGripTemplate}"></Setter>

    <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <Setter Property="Margin" Value="5,4,0,4" />
            </DataTrigger>
        </Style.Triggers></Style>

    <Style x:Key="TaskListScrollButton" TargetType="RepeatButton" BasedOn="{StaticResource TaskListScrollButton}">
        <Setter Property="Template" Value="{DynamicResource FlatRepeatTemplate}"></Setter>
    </Style>

    <Style TargetType="ToggleButton" x:Key="TrayToggleButton" BasedOn="{StaticResource TrayToggleButton}">
        <Setter Property="Background" Value="{DynamicResource TrayBackground}" />
        <Setter Property="Template" Value="{DynamicResource FlatTrayToggleTemplate}"></Setter>

    <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <DataTrigger.Setters>
                    <Setter Property="Margin" Value="-1,-4,-1,-2" />
                </DataTrigger.Setters>
            </DataTrigger>
        </Style.Triggers></Style>

    <BitmapImage x:Key="ShowDesktopIconImageSmall" UriSource="../Resources/desktopxp-sm.png" />
    <BitmapImage x:Key="ShowDesktopIconImageLarge" UriSource="../Resources/desktopxp-lg.png" />

    <Style TargetType="ToggleButton" x:Key="ShowDesktopButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource ButtonFace}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Template" Value="{DynamicResource FlatToggleTemplate}"></Setter>
    </Style>
</ResourceDictionary>