<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:system="clr-namespace:System;assembly=mscorlib">

    <system:Double x:Key="TaskbarHeight">30</system:Double>
    <system:Double x:Key="TaskbarRowHeight">30</system:Double>
    <system:Boolean x:Key="AllowsTransparency">True</system:Boolean>
    <Thickness x:Key="TaskButtonMargin" Right="2" />
    <FontWeight x:Key="SetTimeMenuItemWeight">Normal</FontWeight>

    <SolidColorBrush x:Key="TaskbarBackground" Color="#4fffffff" />
    <SolidColorBrush x:Key="TaskbarBackgroundGlow" Color="#33ffffff" />
    <SolidColorBrush x:Key="TaskbarVerticalBackground" Color="#4fffffff" />
    <SolidColorBrush x:Key="TaskbarVerticalBackgroundGlow" Color="#33ffffff" />
    <SolidColorBrush x:Key="TaskbarTopBorder" Color="#80191e1e" />
    <SolidColorBrush x:Key="TaskbarTopInnerBorder1" Color="#d6ffffff" />
    <SolidColorBrush x:Key="TaskbarTopInnerBorder2" Color="#7dffffff" />
    <SolidColorBrush x:Key="TaskbarBottomBorder" Color="#c7ffffff" />

    <SolidColorBrush x:Key="TrayToggleBorder" Color="#99000000" />
    <SolidColorBrush x:Key="TrayToggleInnerBorder" Color="#e6ffffff" />
    <SolidColorBrush x:Key="TrayToggleBackground" Color="#33ffffff" />
    <SolidColorBrush x:Key="TrayToggleHoverBackground" Color="#66ffffff" />
    <SolidColorBrush x:Key="TrayTogglePressedBackground" Color="#66ffffff" />
    <SolidColorBrush x:Key="TrayToggleArrowForeground" Color="#3d4982" />
    <SolidColorBrush x:Key="TrayToggleArrowPressed" Color="#3d4982" />
    <Thickness x:Key="TrayToggleArrowPadding" Left="4" Top="2" Right="5" Bottom="2" />

    <SolidColorBrush x:Key="TaskButtonOuterBorder" Color="#33000000" />
    <SolidColorBrush x:Key="TaskButtonInnerHorizontalBorder" Color="#80ffffff" />
    <SolidColorBrush x:Key="TaskButtonInnerVerticalBorder" Color="#80ffffff" />
    <SolidColorBrush x:Key="TaskButtonBackground" Color="#4dffffff" />
    <SolidColorBrush x:Key="TaskButtonBottomBackground" Color="Transparent" />

    <SolidColorBrush x:Key="TaskButtonOuterBorderHover" Color="#66000000" />
    <SolidColorBrush x:Key="TaskButtonInnerHorizontalBorderHover" Color="#80ffffff" />
    <SolidColorBrush x:Key="TaskButtonInnerVerticalBorderHover" Color="#80ffffff" />
    <SolidColorBrush x:Key="TaskButtonBackgroundHover" Color="#66ffffff" />
    <SolidColorBrush x:Key="TaskButtonBottomBackgroundHover" Color="Transparent" />

    <SolidColorBrush x:Key="TaskButtonOuterBorderActive" Color="#4d000000" />
    <SolidColorBrush x:Key="TaskButtonInnerHorizontalBorderActive" Color="#80ffffff" />
    <SolidColorBrush x:Key="TaskButtonInnerVerticalBorderActive" Color="#80ffffff" />
    <SolidColorBrush x:Key="TaskButtonBackgroundActive" Color="#66ffffff" />
    <SolidColorBrush x:Key="TaskButtonBottomBackgroundActive" Color="Transparent" />

    <SolidColorBrush x:Key="TaskButtonOuterBorderFlashing" Color="#99000000" />
    <SolidColorBrush x:Key="TaskButtonInnerTopBorderFlashing" Color="#ea9437" />
    <SolidColorBrush x:Key="TaskButtonInnerBottomBorderFlashing" Color="#ce7a1f" />
    <SolidColorBrush x:Key="TaskButtonInnerVerticalBorderFlashing" Color="#e1a25d" />
    <SolidColorBrush x:Key="TaskButtonBackgroundFlashing" Color="#e9902f" />

    <SolidColorBrush x:Key="TaskButtonThumbnailBackground" Color="#4fffffff" />
    <SolidColorBrush x:Key="TaskButtonThumbnailBorder" Color="#80191e1e" />
    <SolidColorBrush x:Key="TaskButtonThumbnailInnerBorder" Color="#88ffffff" />
    <SolidColorBrush x:Key="TaskButtonThumbnailThumbBorder" Color="#aa000000" />
    <DropShadowEffect x:Key="TaskButtonThumbnailShadow" Color="Black" ShadowDepth="0" Direction="270" Opacity="0" BlurRadius="0" />

    <CornerRadius x:Key="TaskListScrollUpCornerRadius">0,0,2,2</CornerRadius>
    <CornerRadius x:Key="TaskListScrollDownCornerRadius">2,2,0,0</CornerRadius>
    <CornerRadius x:Key="TaskListScrollLeftCornerRadius">0,2,2,0</CornerRadius>
    <CornerRadius x:Key="TaskListScrollRightCornerRadius">2,0,0,2</CornerRadius>

    <SolidColorBrush x:Key="TaskListScrollButtonTopBorder" Color="#26000000" />
    <SolidColorBrush x:Key="TaskListScrollButtonLeftBorder" Color="#26ffffff" />
    <SolidColorBrush x:Key="TaskListScrollButtonRightBorder" Color="#26ffffff" />
    <SolidColorBrush x:Key="TaskListScrollButtonBottomBorder" Color="#26ffffff" />
    <SolidColorBrush x:Key="TaskListScrollButtonInnerTopBorder" Color="Transparent" />
    <SolidColorBrush x:Key="TaskListScrollButtonInnerLeftBorder" Color="#33000000" />
    <SolidColorBrush x:Key="TaskListScrollButtonInnerRightBorder" Color="#33000000" />
    <SolidColorBrush x:Key="TaskListScrollButtonInnerBottomBorder" Color="#33000000" />
    <SolidColorBrush x:Key="TaskListScrollButtonOuterLeftBorder" Color="#1f000000" />
    <SolidColorBrush x:Key="TaskListScrollButtonOuterRightBorder" Color="#55000000" />
    <SolidColorBrush x:Key="TaskListScrollButtonOuterBottomBorder" Color="#44000000" />
    <SolidColorBrush x:Key="TaskListScrollButtonBackground" Color="#33000000" />
    <SolidColorBrush x:Key="TaskListScrollButtonBackgroundHover" Color="#26000000" />
    <SolidColorBrush x:Key="TaskListScrollButtonBackgroundPressed" Color="#80000000" />
    <SolidColorBrush x:Key="TaskListScrollDownButtonInnerTopBorder" Color="#33000000" />
    <SolidColorBrush x:Key="TaskListScrollDownButtonOuterTopBorder" Color="#44000000" />
    <SolidColorBrush x:Key="TaskListScrollDownButtonTopBorder" Color="#26ffffff" />
    <SolidColorBrush x:Key="TaskListScrollDownButtonInnerBottomBorder" Color="Transparent" />
    <SolidColorBrush x:Key="TaskListScrollDownButtonBottomBorder" Color="Transparent" />
    <SolidColorBrush x:Key="TaskListScrollArrow" Color="#ffffff" />

    <SolidColorBrush x:Key="ToolbarThumbForeground" Color="#aa000000" />

    <DropShadowEffect x:Key="ForegroundShadow" Color="#000000" BlurRadius="0" ShadowDepth="0" Opacity="0" />

    <SolidColorBrush x:Key="ItemButtonForeground" Color="#ffffff" />
    <SolidColorBrush x:Key="ButtonForeground" Color="#ffffff" />
    <SolidColorBrush x:Key="ButtonActiveForeground" Color="#ffffff" />
    <SolidColorBrush x:Key="ButtonPressedForeground" Color="#ffffff" />
    <SolidColorBrush x:Key="ButtonFlashingForeground" Color="#ffffff" />
    <SolidColorBrush x:Key="ClockForeground" Color="#ffffff" />
    <SolidColorBrush x:Key="InputLanguageForeground" Color="#ffffff" />
    <SolidColorBrush x:Key="InputLanguageBackground" Color="Transparent" />
    <SolidColorBrush x:Key="InputLanguageSelectorForeground" Color="#000000" />

    <SolidColorBrush x:Key="ToolTipBackground" Color="#f4f4f4" />
    <SolidColorBrush x:Key="ToolTipForeground" Color="#00025c" />

    <SolidColorBrush x:Key="BalloonCloseButtonInactiveBackground" Color="#FFFFF5" />
    <SolidColorBrush x:Key="BalloonCloseButtonInactiveBorder" Color="#C7BEA6" />
    <SolidColorBrush x:Key="BalloonCloseButtonInactiveForeground" Color="#AA9C87" />
    <SolidColorBrush x:Key="BalloonCloseButtonInactiveShadow" Color="Transparent" />

    <SolidColorBrush x:Key="BalloonCloseButtonActiveBorder" Color="#FFFFFF" />
    <SolidColorBrush x:Key="BalloonCloseButtonActiveForeground" Color="#FFFFFF" />
    <SolidColorBrush x:Key="BalloonCloseButtonActiveShadowLight" Color="Transparent" />
    <SolidColorBrush x:Key="BalloonCloseButtonActiveShadowDark" Color="Transparent" />

    <SolidColorBrush x:Key="BalloonCloseButtonHoverInnerLight" Color="Transparent" />
    <SolidColorBrush x:Key="BalloonCloseButtonHoverInnerDark" Color="Transparent" />

    <SolidColorBrush x:Key="BalloonCloseButtonHoverBackground" Color="#ffad31" />

    <SolidColorBrush x:Key="BalloonCloseButtonPressedInnerLight" Color="Transparent" />
    <SolidColorBrush x:Key="BalloonCloseButtonPressedInnerDark" Color="Transparent" />

    <SolidColorBrush x:Key="BalloonCloseButtonPressedBackground" Color="#cc7900" />

    <BitmapImage x:Key="StartButtonImage" UriSource="../Resources/startlhaerobutton.png" />
    <BitmapImage x:Key="ShowDesktopIconImageSmall" UriSource="../Resources/desktopxp-sm.png" />
    <BitmapImage x:Key="ShowDesktopIconImageLarge" UriSource="../Resources/desktopxp-lg.png" />

    <Style TargetType="MenuItem" x:Key="TaskManMenuItem">
        <Setter Property="Header" Value="{DynamicResource show_taskman}" />
    </Style>

    <FontFamily x:Key="GlobalFontFamily">Segoe UI</FontFamily>
    <system:Double x:Key="GlobalFontSize">12</system:Double>

    <Style TargetType="Window" x:Key="TaskbarWindow" BasedOn="{StaticResource TaskbarWindow}">
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style TargetType="ContentControl" x:Key="Taskbar" BasedOn="{StaticResource Taskbar}">
        <Setter Property="Template" Value="{DynamicResource FlatPanelTemplate}"></Setter>
    </Style>

    <Style TargetType="GroupBox" x:Key="Tray">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template" Value="{DynamicResource FlatTrayTemplate}"></Setter>
    </Style>

    <Style TargetType="Button" x:Key="TaskButton" BasedOn="{StaticResource TaskButton}">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Template" Value="{DynamicResource FlatTaskTemplate}"></Setter>
    <Setter Property="Padding" Value="6,2" /><Setter Property="Foreground" Value="{DynamicResource FlatTaskText}" /></Style>

    <Style TargetType="Button" x:Key="TaskButtonActive" BasedOn="{StaticResource TaskButton}">
        <Setter Property="Foreground" Value="{DynamicResource FlatTaskText}" />
        <Setter Property="Template" Value="{DynamicResource FlatTaskTemplate}"></Setter>
    <Setter Property="Margin" Value="0" /><Setter Property="Padding" Value="6,2" /></Style>

    <Style TargetType="Button" x:Key="TaskButtonFlashing" BasedOn="{StaticResource TaskButton}">
        <Setter Property="Template" Value="{DynamicResource FlatTaskTemplate}"></Setter>
    <Setter Property="Margin" Value="0" /><Setter Property="Padding" Value="6,2" /><Setter Property="Foreground" Value="{DynamicResource ButtonFlashingForeground}" /></Style>

    <Style TargetType="ContentControl" x:Key="TaskButtonThumbnail">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <Border BorderBrush="{DynamicResource TaskButtonThumbnailBorder}" BorderThickness="1" Background="{DynamicResource TaskButtonThumbnailBackground}" CornerRadius="4" Margin="4" Effect="{DynamicResource TaskButtonThumbnailShadow}">
                        <Border BorderBrush="{DynamicResource TaskButtonThumbnailInnerBorder}" BorderThickness="1" CornerRadius="4" Padding="3">
                            <Border BorderBrush="{DynamicResource TaskButtonThumbnailInnerBorder}" BorderThickness="1" CornerRadius="2">
                                <Border BorderBrush="{DynamicResource TaskButtonThumbnailThumbBorder}" BorderThickness="1" CornerRadius="1">
                                    <ContentPresenter />
                                </Border>
                            </Border>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TaskListScrollUpButton" TargetType="RepeatButton" BasedOn="{StaticResource TaskListScrollButton}">
        <Setter Property="Margin" Value="0,1,0,0" />
        <Setter Property="Template" Value="{DynamicResource FlatRepeatTemplate}"></Setter>

    <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <DataTrigger.Setters>
                    <Setter Property="Margin" Value="0" />
                </DataTrigger.Setters>
            </DataTrigger>
        </Style.Triggers></Style>

    <Style x:Key="TaskListScrollDownButton" TargetType="RepeatButton" BasedOn="{StaticResource TaskListScrollButton}">
        <Setter Property="Margin" Value="0,-1,0,0" />
        <Setter Property="Template" Value="{DynamicResource FlatRepeatTemplate}"></Setter>

    <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <DataTrigger.Setters>
                    <Setter Property="Margin" Value="0" />
                </DataTrigger.Setters>
            </DataTrigger>
        </Style.Triggers></Style>

    <ToolTip x:Key="StartButtonTip" Content="{DynamicResource start_button_tip}" />

    <Style TargetType="ToggleButton" x:Key="StartButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource ItemButtonForeground}" />
        <Setter Property="Margin" Value="0,0,3,0" />
        <Setter Property="Width" Value="78" />
        <Setter Property="Height" Value="30" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template" Value="{DynamicResource FlatToggleTemplate}"></Setter>

    <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <DataTrigger.Setters>
                    <Setter Property="Height" Value="28" />
                    <Setter Property="Margin" Value="0" />
                </DataTrigger.Setters>
            </DataTrigger>
        </Style.Triggers></Style>

    <Style TargetType="Image" x:Key="TaskIcon" BasedOn="{StaticResource TaskIcon}">
        <Setter Property="Margin" Value="0,0,2,1" />
    </Style>

    <Style TargetType="Image" x:Key="TaskOverlayIcon" BasedOn="{StaticResource TaskOverlayIcon}">
        <Setter Property="Margin" Value="0,0,1,0" />
    </Style>

    <Style TargetType="TextBlock" x:Key="TaskLabel" BasedOn="{StaticResource TaskLabel}">
        <Setter Property="Margin" Value="0,0,1,0" />
        <Setter Property="Effect" Value="{StaticResource ForegroundShadow}" />
    </Style>

    <Style TargetType="TextBlock" x:Key="InputLanguage" BasedOn="{StaticResource InputLanguage}">
        <Setter Property="Effect" Value="{StaticResource ForegroundShadow}" />
    </Style>

    <Style TargetType="TextBlock" x:Key="JapaneseIme" BasedOn="{StaticResource JapaneseIme}">
        <Setter Property="Effect" Value="{StaticResource ForegroundShadow}" />
    </Style>

    <Style TargetType="TextBlock" x:Key="Clock" BasedOn="{StaticResource Clock}">
        <Setter Property="Margin" Value="6,2,10,2" />
        <Setter Property="Effect" Value="{StaticResource ForegroundShadow}" />

    <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <DataTrigger.Setters>
                    <Setter Property="Margin" Value="0,3" />
                </DataTrigger.Setters>
            </DataTrigger>
        </Style.Triggers></Style>

    <ControlTemplate TargetType="{x:Type UserControl}" x:Key="ClockTemplateKey">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding Path=Now, StringFormat=t, Mode=OneWay}" Style="{DynamicResource Clock}" Name="ClockText">
            </TextBlock>
            <TextBlock Text="{Binding Path=Now, StringFormat=dddd, Mode=OneWay}" Style="{DynamicResource Clock}" Name="ClockVerticalDayOfWeek" Visibility="Visible">
            </TextBlock>
            <TextBlock Text="{Binding Path=Now, StringFormat=d, Mode=OneWay}" Style="{DynamicResource Clock}" Name="ClockVerticalDate" Visibility="Visible">
            </TextBlock>
        </StackPanel>
        <ControlTemplate.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Horizontal" />
                    <Condition Binding="{Binding Path=Rows, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="1" />
                </MultiDataTrigger.Conditions>
                <MultiDataTrigger.Setters>
                    <Setter TargetName="ClockVerticalDayOfWeek" Property="Visibility" Value="Collapsed" />
                    <Setter TargetName="ClockVerticalDate" Property="Visibility" Value="Collapsed" />
                </MultiDataTrigger.Setters>
            </MultiDataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="Image" x:Key="NotifyIcon" BasedOn="{StaticResource NotifyIcon}">
        <Setter Property="Margin" Value="0,1,4,1" />
    </Style>

    <Style TargetType="ItemsControl" x:Key="NotifyIconItems" BasedOn="{StaticResource NotifyIconItems}">
        <Setter Property="Margin" Value="0,1" />
    </Style>

    <Style TargetType="ItemsControl" x:Key="ToolbarItems" BasedOn="{StaticResource ToolbarItems}">

    <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <DataTrigger.Setters>
                    <Setter Property="Margin" Value="0,3" />
                </DataTrigger.Setters>
            </DataTrigger>
        </Style.Triggers></Style>

    <Style TargetType="Button" x:Key="ToolbarButton" BasedOn="{StaticResource ToolbarButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template" Value="{DynamicResource FlatButtonTemplate}"></Setter>
    </Style>

    <Style TargetType="Thumb" x:Key="ToolbarThumb" BasedOn="{StaticResource ToolbarThumb}">
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Margin" Value="4,6,0,4" />
        <Setter Property="Width" Value="6" />
        <Setter Property="Template" Value="{DynamicResource FlatGripTemplate}"></Setter>

    <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <Setter Property="Margin" Value="5,3,6,1" />
            </DataTrigger>
        </Style.Triggers></Style>

    <Style x:Key="TaskListScrollButtonGrid" TargetType="Grid">
        <Setter Property="Margin" Value="1,0" />
    </Style>

    <Style x:Key="TaskListScrollViewer" TargetType="ScrollViewer" BasedOn="{StaticResource TaskListScrollViewer}">
        <Setter Property="Margin" Value="2,0,10,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ScrollContentPresenter Grid.Column="0" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" CanContentScroll="{TemplateBinding CanContentScroll}" Content="{TemplateBinding Content}" />
                        <Grid Grid.Column="1" Style="{DynamicResource TaskListScrollButtonGrid}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <RepeatButton Command="ScrollBar.PageUpCommand" Style="{DynamicResource TaskListScrollUpButton}" Grid.Column="0">
                                <Path Fill="{DynamicResource TaskListScrollArrow}" Data="{DynamicResource VArrow}" RenderTransformOrigin="0.5,0.5" RenderOptions.EdgeMode="Aliased">
                                    <Path.RenderTransform>
                                        <ScaleTransform ScaleY="-1" />
                                    </Path.RenderTransform>
                                </Path>
                            </RepeatButton>
                            <RepeatButton Command="ScrollBar.PageDownCommand" Style="{DynamicResource TaskListScrollDownButton}" Grid.Row="1">
                                <Path Fill="{DynamicResource TaskListScrollArrow}" Data="{DynamicResource VArrow}" RenderOptions.EdgeMode="Aliased">
                                </Path>
                            </RepeatButton>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <DataTrigger.Setters>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <ScrollContentPresenter Grid.Row="0" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" CanContentScroll="{TemplateBinding CanContentScroll}" Content="{TemplateBinding Content}" />
                                    <Grid Grid.Row="1" Style="{DynamicResource TaskListScrollButtonGrid}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <RepeatButton Command="ScrollBar.PageUpCommand" Style="{DynamicResource TaskListScrollUpButton}" Padding="7" Grid.Column="0">
                                            <Path Fill="{DynamicResource TaskListScrollArrow}" Data="{DynamicResource VArrow}" Margin="7" RenderTransformOrigin="0.5,0.5" RenderOptions.EdgeMode="Aliased">
                                                <Path.RenderTransform>
                                                    <ScaleTransform ScaleY="-1" />
                                                </Path.RenderTransform>
                                            </Path>
                                        </RepeatButton>
                                        <RepeatButton Command="ScrollBar.PageDownCommand" Style="{DynamicResource TaskListScrollDownButton}" Padding="4" Grid.Column="1">
                                            <Path Fill="{DynamicResource TaskListScrollArrow}" Data="{DynamicResource VArrow}" Margin="7" RenderOptions.EdgeMode="Aliased">
                                            </Path>
                                        </RepeatButton>
                                    </Grid>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Margin" Value="0,0,0,10" />
                </DataTrigger.Setters>
            </DataTrigger>
        </Style.Triggers></Style>

    <Geometry x:Key="VArrow">M 4.5 6 L 9 1.5 L 8 0 L 4.5 3 L 1 0 L 0 1.5 Z</Geometry>
    <Geometry x:Key="HArrow">M 0,4.5 L 4.5,9 L 4.5,0 Z</Geometry>

    <Style TargetType="ToggleButton" x:Key="TrayToggleButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource TrayToggleBackground}" />
        <Setter Property="Margin" Value="0,1,3,0" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template" Value="{DynamicResource FlatTrayToggleTemplate}"></Setter>

    <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="ToolTip" Value="{DynamicResource hide}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="False">
                <Setter Property="ToolTip" Value="{DynamicResource show_hidden}" />
            </Trigger>
            <DataTrigger Binding="{Binding Path=Orientation, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Value="Vertical">
                <DataTrigger.Setters>
                    <Setter Property="Margin" Value="0,0,0,1" />
                </DataTrigger.Setters>
            </DataTrigger>
        </Style.Triggers></Style>

    <Style TargetType="ToggleButton" x:Key="ShowDesktopButton">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template" Value="{DynamicResource FlatToggleTemplate}"></Setter>
    </Style>

    <Style TargetType="Button" x:Key="NotifyBalloonCloseButton" BasedOn="{StaticResource NotifyBalloonCloseButton}">
        <Setter Property="Foreground" Value="{DynamicResource BalloonCloseButtonInactiveForeground}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border BorderBrush="{DynamicResource BalloonCloseButtonInactiveShadow}" BorderThickness="0,0,1,1" CornerRadius="3" Name="BorderShadowDark">
                        <Border BorderBrush="{DynamicResource BalloonCloseButtonInactiveShadow}" BorderThickness="1,1,0,0" CornerRadius="3" Name="BorderShadowLight">
                            <Border BorderBrush="{DynamicResource BalloonCloseButtonInactiveBorder}" BorderThickness="1" CornerRadius="2" Background="{DynamicResource BalloonCloseButtonInactiveBackground}" Name="Border">
                                <Border BorderBrush="{DynamicResource BalloonCloseButtonInactiveShadow}" BorderThickness="0,0,1,1" CornerRadius="2" Name="BorderInnerDark">
                                    <Border BorderBrush="{DynamicResource BalloonCloseButtonInactiveShadow}" BorderThickness="1" CornerRadius="2" Padding="3,3,0,0" Name="BorderInnerLight">
                                        <Grid x:Name="Content">
                                            <Path Stroke="{TemplateBinding Foreground}" StrokeEndLineCap="Round" StrokeStartLineCap="Round" Width="8" Height="8" StrokeThickness="2" Data="M 0,0 l 6,6" />
                                            <Path Stroke="{TemplateBinding Foreground}" StrokeEndLineCap="Round" StrokeStartLineCap="Round" Width="8" Height="8" StrokeThickness="2" Data="M 0,6 l 6,-6" />
                                        </Grid>
                                    </Border>
                                </Border>
                            </Border>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BorderShadowDark" Property="BorderBrush" Value="{DynamicResource BalloonCloseButtonActiveShadowDark}" />
                            <Setter TargetName="BorderShadowLight" Property="BorderBrush" Value="{DynamicResource BalloonCloseButtonActiveShadowLight}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource BalloonCloseButtonActiveBorder}" />
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource BalloonCloseButtonHoverBackground}" />
                            <Setter TargetName="BorderInnerDark" Property="BorderBrush" Value="{DynamicResource BalloonCloseButtonHoverInnerDark}" />
                            <Setter TargetName="BorderInnerLight" Property="BorderBrush" Value="{DynamicResource BalloonCloseButtonHoverInnerLight}" />
                            <Setter Property="Foreground" Value="{DynamicResource BalloonCloseButtonActiveForeground}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="BorderShadowDark" Property="BorderBrush" Value="{DynamicResource BalloonCloseButtonActiveShadowDark}" />
                            <Setter TargetName="BorderShadowLight" Property="BorderBrush" Value="{DynamicResource BalloonCloseButtonActiveShadowLight}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource BalloonCloseButtonActiveBorder}" />
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource BalloonCloseButtonPressedBackground}" />
                            <Setter TargetName="BorderInnerDark" Property="BorderBrush" Value="{DynamicResource BalloonCloseButtonPressedInnerDark}" />
                            <Setter TargetName="BorderInnerLight" Property="BorderBrush" Value="{DynamicResource BalloonCloseButtonPressedInnerLight}" />
                            <Setter TargetName="BorderInnerDark" Property="BorderThickness" Value="1,1,0,0" />
                            <Setter Property="Foreground" Value="{DynamicResource BalloonCloseButtonActiveForeground}" />
                            <Setter TargetName="Content" Property="Opacity" Value="0.5" />
                            <Setter TargetName="Content" Property="Margin" Value="-1,-1,0,0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>